# FastAPI Microservices Project

Выполнил: Суворов Даниил Сергеевич
Группа: РИМ-150937к

Учебный проект, состоящий из двух микросервисов, реализованных с использованием **FastAPI**, **SQLite** и **Docker**.

Проект демонстрирует:

- разработку REST API;
- работу с базой данных SQLite;
- контейнеризацию сервисов с использованием Docker;
- хранение данных в именованных Docker-томах.

---

## 📂 Структура репозитория
<pre>
├── todo_app/
│   ├── app/
│   ├── Dockerfile
│   ├── requirements.txt
│   └── README.md
│
├── shorturl_app/
│   ├── app/
│   ├── Dockerfile
│   ├── requirements.txt
│   └── README.md
│
└── README.md
</pre>

---

## 🚀 Сервисы

### ✅ 1. ToDo-сервис (`todo_app`)

Сервис для управления списком задач с поддержкой CRUD-операций.

**Функциональность:**

- создание задачи;
- получение списка задач;
- обновление задачи;
- удаление задачи.

**Хранилище:** SQLite  
**Порт:** `8000`

---

### 🔗 2. Сервис сокращения URL (`shorturl_app`)

Сервис для создания коротких ссылок и перенаправления на исходные URL.

**Эндпоинты:**

- `POST /shorten` — создать короткую ссылку
- `GET /{short_id}` — перенаправление на исходный URL
- `GET /stats/{short_id}` — информация о ссылке

**Хранилище:** SQLite  
**Порт:** `8001`

---

## 🐳 Запуск через Docker

### 1️⃣ Создать именованные тома

```bash
2️⃣ Создание томов
docker volume create todo_data
docker volume create shorturl_data


2️⃣ ToDo-сервис

cd todo_app
docker build -t todo-service .
docker run -d -p 8000:80 -v todo_data:/app/app/data todo-service


3️⃣ Short URL сервис

cd shorturl_app
docker build -t shorturl-service .
docker run -d -p 8001:80 -v shorturl_data:/app/app/data shorturl-service
```

🛠 Используемые технологии:
- Python 3.11
- FastAPI
- SQLAlchemy
- SQLite
- Docker
- Uvicorn


## 📦 Docker Hub

Все сервисы проекта доступны на Docker Hub:

- **ToDo-сервис**: [dannil1298/todo-service](https://hub.docker.com/repository/docker/dannil1298/todo-service)  
- **Сервис сокращения URL**: [dannil1298/shorturl-service](https://hub.docker.com/repository/docker/dannil1298/shorturl-service)  

### 🔽 Скачивание образов

```bash
docker pull dannil1298/todo-service:latest
docker pull dannil1298/shorturl-service:latest
